---
description: Generate a custom pre-merge checklist for the current feature based on its spec, plan, and FURQAN's Pre-merge constitutional requirements.
---

Invoke the `speckit-checklist` skill. The output should fold the constitution's Pre-merge checklist (CI green, migration dry-run, `/speckit.analyze` clean) into a feature-specific list — for example, "Confirm `/admin/control-tower` Murajaah widget is gated behind admin role" alongside generic items.
